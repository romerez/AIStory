# AIStory local stack environment template.
# Copy to .env in the project root if you want to set custom endpoints
# via environment instead of the Settings UI.

# Image proxy
IMAGE_PROXY_PORT=8989
COMFY_URL=http://127.0.0.1:8188

# Ollama (defaults are fine; override only if you moved it)
# OLLAMA_BASE_URL=http://127.0.0.1:11434
